name: tyro_counter
description: "A counter application to grasps bloc better"
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ^3.10.7

dependencies:
  flutter:
    sdk: flutter

  get_it: ^9.2.0
  injectable: ^2.7.1+4
  cupertino_icons: ^1.0.8
  go_router: ^17.0.1
  flutter_bloc: ^9.1.1


dev_dependencies:
  flutter_test:
    sdk: flutter

  build_runner: ^2.4.0
  injectable_generator: ^2.12.0


  flutter_lints: ^6.0.0

flutter:

  uses-material-design: true

